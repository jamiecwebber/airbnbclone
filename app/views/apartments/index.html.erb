<h1>Your Listed Apartments</h1>
<p>Here is your current list of apartments: </p>

 <div class="row">
     <div class="card">
      <div class="col-sm-12">
        <div class = "list">
          <ul>
            <% @apartments.each do |apartment| %>
                <li><%= link_to apartment.name, apartment_path(user.id)%></li>
              <% end %>
            </ul>

            <div class="btn">
              <%= link_to "Apartment", apartments_path %>
            </div>
            </div>
          </div>
        </div>
